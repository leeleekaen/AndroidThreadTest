<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky.androidthreadapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mythread.HandlerThreadTestActivity"/>
        <activity android:name=".mythread.IntentServiceTestActivity"/>
        <activity android:name=".mythread.MyAsyncTaskActivity"/>
        <activity android:name=".mythread.ThreadPoolTestActivity"/>
        <activity android:name=".mythread.ThreadStopTestActivity"/>
        <activity android:name=".mythread.ThreadTestActivity"/>
    </application>

</manifest>